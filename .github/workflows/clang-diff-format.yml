name: Clang Diff For

on:
  pull_request:
    branches:
      - develop
jobs:
  ClangFormat:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Run Clang-Format-Diff.py
        run: git fetch && git diff -U0 origin/develop | ./contrib/devtools/clang-format-diff.py -p1

